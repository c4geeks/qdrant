# Copy to .env and adjust.
# Detect with: curl -s https://api.github.com/repos/qdrant/qdrant/releases/latest | jq -r .tag_name
QDRANT_VERSION=v1.18.1
# Generate with: openssl rand -base64 32
QDRANT_API_KEY=replace-me-with-a-long-random-string
